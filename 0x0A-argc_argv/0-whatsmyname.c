#include <stdio.h>

/**
 * main - A program to print its name, followed by a new line.
 * @argc: The Number of element in a command line
 * @argv: The array of elements
 * Return: 0 Success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
